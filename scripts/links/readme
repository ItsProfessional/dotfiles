path/ contains links for the purpose of putting programs that aren't in path (e.g. portable programs, etc.) to path
aliases/ contains aliases (that work without a shell)
overrides/ contains links similar to those in aliases/ but these override other programs. E.g. dmenu -> rofi

